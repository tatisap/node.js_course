export const FIRST_PRIME = 2;
export const REQUIRED_NUMBER_OF_ARGS = 2;
export const ARG_SEPARATOR = '=';
export const NO_CONTENT = '';

export const ARGS_NAMES = {
  numberOfPrimes: '--number-of-primes',
  maxValue: '--max-value',
};

export const MESSAGES = {
  notEnoughArgs: 'Two arguments are required',
  sameNameArgs: 'Same name arguments are found',
  noSuchArg: 'Requires argument',
  wrongType: 'Arguments must be a natural number',
};
